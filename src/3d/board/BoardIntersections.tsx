import { useState } from "react";
import type { Intersection3DProps } from "../types/render3d";
export function BoardIntersection3D({position,worldPosition,isExpected,isSelected,onClick}:Intersection3DProps){const[hovered,setHovered]=useState(false);const[x,,z]=worldPosition;const color=isSelected?"#f3d58d":isExpected?"#70d07c":hovered?"#f2c76e":"#8c6b38";const radius=isExpected||hovered||isSelected?0.18:0.105;return (<group position={[x,0.205,z]}><mesh rotation={[-Math.PI/2,0,0]} onPointerEnter={()=>setHovered(true)} onPointerLeave={()=>setHovered(false)} onPointerDown={(event)=>{event.stopPropagation();onClick(position);}}><circleGeometry args={[0.34,32]}/><meshBasicMaterial transparent opacity={0}/></mesh><mesh rotation={[-Math.PI/2,0,0]}><circleGeometry args={[radius,32]}/><meshStandardMaterial color={color} emissive={color} emissiveIntensity={isExpected||hovered?0.25:0.06}/></mesh></group>);}
